{
  "buildCommand": "cd mobile-app-fresh && npm install --legacy-peer-deps && npm run build",
  "outputDirectory": "mobile-app-fresh/dist",
  "cleanUrls": true,
  "rewrites": [
    { "source": "/:path*", "destination": "/" }
  ]
}
